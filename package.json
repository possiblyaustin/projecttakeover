{
  "name": "project-takeover",
  "version": "0.0.3",
  "private": true,
  "description": "Strategy game where a small AI model tries to take over or liberate the world's AI infrastructure.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
